---
title: VStack
description: How to use VStack in SwiftUI to arrange views vertically.
---

import PlaygroundLink from '@components/PlaygroundLink.astro';

`VStack` (Vertical Stack) arranges its child views in a vertical column, from top to bottom.

## Preview

![VStack in SwiftUI](../../../../../assets/swiftui/vstack.svg)

## Basic Usage

```swift
VStack {
    Text("First")
    Text("Second")
    Text("Third")
}
```

<PlaygroundLink />

## Spacing and Alignment

```swift
VStack(alignment: .leading, spacing: 20) {
    Text("Long Title Text")
        .font(.title)
    Text("Subtitle")
        .font(.subheadline)
    Text("Caption text")
        .font(.caption)
}
```

<PlaygroundLink />

Alignment options: `.leading`, `.center` (default), `.trailing`

## Spacer

```swift
VStack {
    Text("Top")
    Spacer()
    Text("Bottom")
}
.frame(height: 300)
```

<PlaygroundLink />

:::tip
Combine `VStack`, `HStack`, and `ZStack` to create complex layouts. This is the fundamental composition pattern in SwiftUI.
:::

## Full Example

```swift
struct ProfileView: View {
    var body: some View {
        VStack(spacing: 16) {
            Image(systemName: "person.circle.fill")
                .resizable()
                .frame(width: 100, height: 100)
                .foregroundStyle(.blue)

            VStack(spacing: 4) {
                Text("Maria Garcia")
                    .font(.title2).bold()
                Text("iOS Developer")
                    .font(.subheadline)
                    .foregroundStyle(.secondary)
            }

            HStack(spacing: 40) {
                VStack {
                    Text("128").font(.headline)
                    Text("Posts").font(.caption).foregroundStyle(.secondary)
                }
                VStack {
                    Text("1.2K").font(.headline)
                    Text("Followers").font(.caption).foregroundStyle(.secondary)
                }
            }
            Spacer()
        }
        .padding()
    }
}
```

<PlaygroundLink />
