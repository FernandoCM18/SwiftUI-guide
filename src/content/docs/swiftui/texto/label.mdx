---
title: Label
description: Aprende a usar el componente Label de SwiftUI para mostrar texto con iconos del sistema.
---

import PlaygroundLink from '@components/PlaygroundLink.astro';

## El componente Label

`Label` combina un texto con un icono, creando un elemento visual consistente ideal para menús, listas y botones. Utiliza SF Symbols para los iconos del sistema.

![Label](../../../../assets/swiftui/label.svg)

## Uso básico

```swift
import SwiftUI

struct LabelBasico: View {
    var body: some View {
        VStack(alignment: .leading, spacing: 16) {
            Label("Favoritos", systemImage: "star.fill")
            Label("Compartir", systemImage: "square.and.arrow.up")
            Label("Ajustes", systemImage: "gearshape")
            Label("Perfil", systemImage: "person.circle")
        }
    }
}
```

<PlaygroundLink />

## Label con estilos

SwiftUI proporciona varios estilos predefinidos para Labels.

```swift
import SwiftUI

struct LabelEstilos: View {
    var body: some View {
        VStack(spacing: 20) {
            // Estilo por defecto: icono + texto
            Label("Automático", systemImage: "wand.and.stars")
                .labelStyle(.automatic)

            // Solo icono
            Label("Solo icono", systemImage: "heart.fill")
                .labelStyle(.iconOnly)

            // Solo título
            Label("Solo título", systemImage: "heart.fill")
                .labelStyle(.titleOnly)

            // Título y icono (explícito)
            Label("Ambos", systemImage: "bolt.fill")
                .labelStyle(.titleAndIcon)
        }
        .font(.title3)
    }
}
```

<PlaygroundLink />

## Personalizar colores y tamaños

```swift
import SwiftUI

struct LabelPersonalizado: View {
    var body: some View {
        VStack(spacing: 16) {
            Label("Importante", systemImage: "exclamationmark.triangle.fill")
                .font(.headline)
                .foregroundStyle(.orange)

            Label("Correcto", systemImage: "checkmark.circle.fill")
                .font(.title2)
                .foregroundStyle(.green)

            Label("Error", systemImage: "xmark.octagon.fill")
                .font(.title3)
                .foregroundStyle(.red)

            Label("Información", systemImage: "info.circle.fill")
                .font(.body)
                .foregroundStyle(.blue)
        }
    }
}
```

<PlaygroundLink />

## Label con contenido personalizado

Puedes crear un Label con vistas personalizadas para el título y el icono.

```swift
import SwiftUI

struct LabelCustom: View {
    var body: some View {
        VStack(spacing: 20) {
            // Label con vistas personalizadas
            Label {
                Text("Descarga completada")
                    .font(.headline)
            } icon: {
                Image(systemName: "arrow.down.circle.fill")
                    .foregroundStyle(.green)
                    .font(.title)
            }

            // Label con múltiples líneas de texto
            Label {
                VStack(alignment: .leading) {
                    Text("María García")
                        .font(.headline)
                    Text("Desarrolladora iOS")
                        .font(.caption)
                        .foregroundStyle(.secondary)
                }
            } icon: {
                Image(systemName: "person.crop.circle.fill")
                    .font(.largeTitle)
                    .foregroundStyle(.blue)
            }
        }
    }
}
```

<PlaygroundLink />

## Label en listas

Los Labels son especialmente útiles dentro de listas y menús.

```swift
import SwiftUI

struct LabelEnLista: View {
    var body: some View {
        List {
            Section("General") {
                Label("Notificaciones", systemImage: "bell.badge")
                Label("Sonidos", systemImage: "speaker.wave.2")
                Label("Enfoque", systemImage: "moon.fill")
            }

            Section("Privacidad") {
                Label("Localización", systemImage: "location")
                Label("Cámara", systemImage: "camera")
                Label("Micrófono", systemImage: "mic")
            }

            Section("Cuenta") {
                Label("iCloud", systemImage: "icloud")
                Label("Suscripciones", systemImage: "creditcard")
            }
        }
    }
}
```

<PlaygroundLink />

## Label como botón

```swift
import SwiftUI

struct LabelBoton: View {
    @State private var esFavorito = false

    var body: some View {
        VStack(spacing: 20) {
            Button {
                esFavorito.toggle()
            } label: {
                Label(
                    esFavorito ? "Quitar de favoritos" : "Añadir a favoritos",
                    systemImage: esFavorito ? "star.fill" : "star"
                )
                .foregroundStyle(esFavorito ? .yellow : .gray)
            }

            Button {
                // Acción de compartir
            } label: {
                Label("Compartir", systemImage: "square.and.arrow.up")
                    .padding()
                    .background(Color.blue)
                    .foregroundStyle(.white)
                    .clipShape(.rect(cornerRadius: 10))
            }
        }
    }
}
```

<PlaygroundLink />

:::tip
SF Symbols ofrece miles de iconos gratuitos. Descarga la app SF Symbols de Apple para explorar todos los iconos disponibles y sus variantes.
:::

## Resumen

| Propiedad | Descripción |
|-----------|-------------|
| `Label("texto", systemImage:)` | Crea un label con icono del sistema |
| `.labelStyle(.iconOnly)` | Muestra solo el icono |
| `.labelStyle(.titleOnly)` | Muestra solo el texto |
| `.labelStyle(.titleAndIcon)` | Muestra ambos explícitamente |
| `Label { } icon: { }` | Label con contenido personalizado |
