---
title: Introducción a Swift
description: Visión general del lenguaje Swift, su historia, características de seguridad y lo que aprenderás.
sidebar:
  order: 0
---

import PlaygroundLink from '@components/PlaygroundLink.astro';

import { LinkCard, CardGrid } from '@astrojs/starlight/components';

## ¿Qué es Swift?

Swift es un lenguaje de programación moderno, seguro y de alto rendimiento desarrollado por Apple. Fue presentado en la WWDC de 2014 como el sucesor de Objective-C para el desarrollo de aplicaciones en las plataformas Apple: iOS, macOS, watchOS, tvOS y visionOS.

## Historia breve

Swift nació de la necesidad de un lenguaje más moderno y accesible. Chris Lattner comenzó su diseño en 2010 y, desde su lanzamiento público, ha evolucionado rápidamente:

- **Swift 1.0 (2014)**: Lanzamiento inicial.
- **Swift 2.0 (2015)**: Se convierte en código abierto.
- **Swift 3.0 (2016)**: Gran rediseño de la API.
- **Swift 4.0 (2017)**: Codable, Strings mejorados.
- **Swift 5.0 (2019)**: Estabilidad de ABI.
- **Swift 5.5+ (2021)**: Concurrencia moderna con async/await.
- **Swift 6.0 (2024)**: Seguridad de concurrencia estricta.

## Características principales

### Seguridad de tipos
Swift es un lenguaje fuertemente tipado. El compilador detecta errores de tipos en tiempo de compilación, lo que previene muchos bugs comunes.

```swift
// Esto genera un error de compilación
// let numero: Int = "hola"

// Esto es correcto
let numero: Int = 42
```

<PlaygroundLink />

### Manejo de nulos seguro
El sistema de opcionales de Swift obliga a manejar explícitamente los valores que pueden ser `nil`, eliminando los errores de referencia nula.

```swift
// Un valor que puede ser nil
var nombre: String? = "María"

// Acceso seguro al valor
if let nombreSeguro = nombre {
    print("Hola, \(nombreSeguro)")
}
```

<PlaygroundLink />

### Inferencia de tipos
Swift puede inferir automáticamente el tipo de una variable a partir de su valor inicial.

```swift
let mensaje = "Hola mundo"  // Swift infiere que es String
let pi = 3.14159            // Swift infiere que es Double
let activo = true           // Swift infiere que es Bool
```

<PlaygroundLink />

### Rendimiento
Swift está compilado con LLVM y ofrece un rendimiento comparable a C/C++ en muchas tareas.

:::tip[¿Vienes de React + TypeScript?]
Cada tema incluye tabs con el equivalente en TypeScript para que puedas comparar la sintaxis de Swift con lo que ya conoces.
:::

## ¿Qué aprenderás?

En esta sección cubriremos los fundamentos del lenguaje Swift:

<CardGrid>
  <LinkCard title="Variables y Constantes" href="./variables-y-constantes/" description="Declaración con `let` y `var`, inferencia de tipos y anotaciones." />
  <LinkCard title="Tipos de Datos" href="./tipos-de-datos/" description="Int, Double, String, Bool, Array, Dictionary, Set y Tuple." />
  <LinkCard title="Flujo de Control" href="./flujo-de-control/" description="if/else, switch, for-in, while, guard." />
  <LinkCard title="Funciones" href="./funciones/" description="Declaración, parámetros, retornos y closures." />
  <LinkCard title="Structs y Clases" href="./structs-y-clases/" description="Tipos de valor vs referencia, propiedades y métodos." />
  <LinkCard title="Opcionales" href="./opcionales/" description="Optional binding, nil coalescing y optional chaining." />
  <LinkCard title="Protocolos" href="./protocolos/" description="Declaración, conformidad y programación orientada a protocolos." />
  <LinkCard title="Enumeraciones" href="./enumeraciones/" description="Valores crudos, valores asociados y CaseIterable." />
</CardGrid>
